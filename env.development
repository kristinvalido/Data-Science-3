# ── LOCAL DEVELOPMENT (.env.development) ─────────────────────────────────────
# Vite proxy (in vite.config.js) forwards /api/* to the Functions emulator.
# So locally, the base URL is just /api.
VITE_FUNCTIONS_BASE_URL=/api
