# ── PRODUCTION (.env.production) ─────────────────────────────────────────────
# After running `firebase deploy --only functions`, copy the base URL shown
# in the terminal here. It will look like the line below.
VITE_FUNCTIONS_BASE_URL=https://us-central1-titan-tunes-37528.cloudfunctions.net
